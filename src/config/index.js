const {
  MONGO_USERNAME,
  MONGO_PASSWORD,
  MONGO_HOSTNAME,
  MONGO_PORT,
  MONGO_DB,
  APP_PORT,
  LOGIN_SECRET,
  JWT_THRESHOLD
} = process.env;

module.exports = {
  APP_PORT,
  MONGO_USERNAME,
  MONGO_PASSWORD,
  MONGO_HOSTNAME,
  MONGO_PORT,
  MONGO_DB,
  LOGIN_SECRET,
  JWT_THRESHOLD
};
